/* A mean to access to a persistant, immutable configuration at startup.
 * This is recommended tu use commands instead of static configuration file
 * for configuration purpose, so that configuration can be change in real time
 * while the system is running.
 */
